# Verificación — Paso 2 (lección) · 2026-09-12

Artefacto: `D:\learning\lessons\gcp-data-engineering\0002-lo-que-de-verdad-lee-una-consulta.md`

## Veredicto

**PASA CON RESERVAS**

No hay ningún error de hecho. Todas las afirmaciones técnicas sobre BigQuery que comprobé son
correctas según la documentación oficial vigente, y **todos** los números medidos coinciden con la
evidencia del laboratorio (los de clustering los reproduje con mis propios jobs, no los leí del
JSON). Las reservas son dos **citas literales atribuidas a una página que no las contiene**: el
hecho es cierto y está documentado, pero el enlace que acompaña a la cita no lo sostiene. Eso
incumple los puntos 1 y 2 del criterio, aunque no engaña al lector sobre el fondo.

## Criterio de aceptación

1. **Cada afirmación técnica sobre Google Cloud** (comportamiento de un servicio, límite, precio, estado GA o Preview, nombre, rol, permiso o mensaje de error) es correcta según la **documentación oficial vigente** y lleva enlace a esa documentación.
2. **Las citas literales en inglés** (entre comillas y en cursiva) aparecen tal cual, o con el mismo sentido, en la página que enlazan, y se usan en su contexto.
3. Lo que el texto marca como *(inferencia)* es una deducción razonable y no contradice la documentación. Lo que marca con ⚠️ está de verdad sin confirmar, o bien puedes confirmarlo o refutarlo con fuente.
4. Nada se afirma como hecho sin fuente, salvo lo marcado como medido u observado en el laboratorio.
5. **Los números medidos que cita la lección coinciden con la evidencia del laboratorio** (ver más abajo). Si un número no cuadra, es un hallazgo.
6. Formato: quiz con callouts plegables `> [!question]-`, coherente con el cuerpo. Nivel senior: no explica conceptos genéricos de ingeniería, explica lo específico de BigQuery.

## Método

Las once páginas citadas se descargaron con `curl -sL` y se convirtieron a texto plano; cada cita
literal se buscó como subcadena sobre ese texto (normalizando comillas y apóstrofos tipográficos),
no vía resumen. Las cuatro mediciones de clustering se reprodujeron con `bq query --nouse_cache` y
`bq show -j` sobre `sdag-lab-000000.banca.transacciones`, con la configuración `sdag-lab`
(`gcloud config list` → `configuracion: sdag-lab`, `account = you@example.com`). Solo
lecturas: `bq show`, `bq version`, `bq --help` y consultas `SELECT`. No se creó, modificó ni borró
ningún recurso, y no se tocó la configuración `default`.

## Comprobaciones ejecutadas

### A. Números medidos contra la evidencia del laboratorio (criterio 5)

| Afirmación de la lección (sección) | Fuente consultada | Resultado | Evidencia |
|---|---|---|---|
| §1 · 1.957.639 filas | `bq show` de la tabla | OK | `"numRows": "1957639"` |
| §1 · 90 particiones diarias | `bq show` | OK | `"numPartitions": "90"`; `timePartitioning.type = DAY`, `field = fecha` |
| §1 · 163 MB | `bq show` | OK | `numBytes = 162765153` = 162,77 MB decimales → 163 MB |
| §1 · `PARTITION BY fecha` · `CLUSTER BY cuenta_id, tipo` · `require_partition_filter = TRUE` | `bq show` | OK | `clustering.fields = ["cuenta_id","tipo"]`; `requirePartitionFilter: true` |
| §1 · dataset `banca` multirregión US | `bq show …:banca` | OK | `"location": "US"` |
| §1 · sin PII (identificadores internos, importes, tipo, canal, país) | esquema + `generar_transacciones.py` | OK | 9 columnas, `cuenta_id` = `CU%07d` sintético; sin nombres ni documentos |
| §3 · 1 día → `totalPartitionsProcessed` = 1; 966.472 proc.; 10.485.760 fact. | `resultados_poda.json` [1] + informe `2-2026-09-12.md` (reproducido con job propio) | OK | Coincide al byte |
| §3 · 90 días → 90 particiones; 68.515.449 proc.; 69.206.016 fact. | ídem [2] | OK | Coincide al byte |
| §3 · «Un día lee el **1,41 %**» | aritmética | OK | 966.472 / 68.515.449 = **1,4106 %** |
| §3 · el `EXTRACT(MONTH…)` falla con el mismo error que no poner filtro | `resultados_poda.json` [3] y [4] | OK | Ambos devuelven literalmente el mismo mensaje |
| §3 · texto del error real, con nombre de columna | `resultados_poda.json` [3]/[4] | OK | `…without a filter over column(s) 'fecha' that can be used for partition elimination` |
| §4 · 10.485.760 = 10 MiB exactos | aritmética | OK | 10 × 1.048.576 = 10.485.760 |
| §5 · 1 partición: 1.572.459 sin filtro y 1.572.459 con filtro → **0 %** | **reproducido** (`vl2_…_p5` / `_p6`, `--nouse_cache`) | OK | `parts=1 proc=1572459 bill=10485760 cache=False` en ambas |
| §5 · 90 particiones: 111.585.423 → 107.431.263 → **3,7 %** | **reproducido** (`vl2_…_c1` / `_c2`) | OK | `parts=90 proc=111585423` y `proc=107431263`; ahorro = **3,723 %** |
| §5 · «1 partición (1,5 MB)» | aritmética | Imprecisa (menor) | 1.572.459 B = 1,50 **MiB** pero 1,57 MB decimales; el resto de la lección usa MB decimales |
| §5 · «90 particiones (112 MB)» | aritmética | OK | 111.585.423 B = 111,6 MB → 112 MB |
| §7 · CSV de 172,8 MB | `ls`/`os.path.getsize` | OK | `transacciones.csv` = 172.802.717 B = 172,80 MB |
| §7 · carga en 1 min 19 s, exit 0 | `docs/progreso.md:210` | Sin confirmar (observado) | Único respaldo es la bitácora del autor; no hay log de job en `labs/paso-2/` |
| §7 · el ACL del dataset muestra `"role": "WRITER"` | `bq show --format=prettyjson …:banca` | OK | `{"role": "WRITER", "userByEmail": "sdag-pipeline@…"}` |
| §7 · `bq --help` no muestra ningún flag de impersonación | `bq --help \| grep -i impersonat` | OK (confirmado) | 0 coincidencias; `bq version` = 2.1.29 |
| §8 · tabla creada sin expiración | `bq show` tabla y dataset | OK | Sin `expirationMs` ni `defaultPartitionExpirationMs` |
| §9 · «one day scanned 1.4% … one partition versus ninety» | aritmética | OK | 1,41 % · 1 vs 90 |

### B. Citas literales y afirmaciones de documentación (criterios 1 y 2)

| Afirmación de la lección (sección) | Fuente consultada | Resultado | Evidencia |
|---|---|---|---|
| §2 · *"the number of bytes processed by each query"* | [Pricing](https://cloud.google.com/bigquery/pricing) | OK | Literal: «you are charged for the number of bytes processed by each query» |
| §2 · «El primer TiB al mes es gratis» | Pricing | OK | «The first 1 TiB of query data processed per month is free» |
| §2 · *"compute capacity … measured in slots (virtual CPUs) over time"* atribuido a **Editions** | [Editions](https://docs.cloud.google.com/bigquery/docs/editions-intro) + Pricing | Imprecisa (atribución) | **No** está en editions-intro (0 coincidencias de «virtual CPU» / «measured in slots»). Sí, literal, en Pricing: «you are charged for compute capacity used to run queries, measured in slots (virtual CPUs) over time» |
| §2 · «$6.25 por TiB en las multirregiones US y EU» | Pricing | OK | Una sola tabla «Queries (on-demand)»: «$6.25 / 1 tebibyte»; el selector de región de esa tabla incluye tanto `US (us)` como `Europe (eu)`. Los otros precios por TiB de la página son de otros productos ($9.125 = BigQuery Omni; $1.10 = Storage Read API) |
| §2 · *"up to 2,000 concurrent slots, shared among all queries in a single project"* | Pricing | OK | Literal |
| §2 · *"You can choose to use editions and the on-demand model at the same time on a per-project basis."* | Editions | OK | Literal, en editions-intro |
| §2 · *(inferencia)* mal filtrado duele en los dos modelos | — | OK | Deducción razonable; no contradice nada. Coherente con «up to 2,000 concurrent slots, shared among all queries» |
| §3 · *"If a query uses a qualifying filter on the value of the partitioning column, BigQuery can scan the partitions that match the filter and skip the remaining partitions"* | [Partitioned tables](https://docs.cloud.google.com/bigquery/docs/partitioned-tables) | OK | Literal. «act. 2026-09-03» → la página dice `Last updated 2026-09-03 UTC` |
| §3 · «no podan las subconsultas, comparar con otra columna, la aritmética sobre la columna, un `OR` con otra columna» | [Querying partitioned tables](https://docs.cloud.google.com/bigquery/docs/querying-partitioned-tables) | OK | Los cuatro casos aparecen: subconsulta `WHERE t1.ts = (SELECT …)`; `WHERE ts >= ts2`; `WHERE customer_id + 1 BETWEEN 30 AND 50`; y «the following is not sufficient…: `WHERE partition_id = "20221231" OR f = "20221130"`» |
| §3 · *"Other functions and complex mathematical operations will require a full table scan."* | Querying partitioned tables | OK | Literal. Contexto correcto: cierra la lista de funciones que sí podan (entre ellas `EXTRACT with DATE or YEAR parts`, que no incluye `MONTH` — coherente con el experimento) |
| §3 · *"There must be at least one predicate that only references a partitioning column for the filter to be considered eligible for partition elimination"* | Querying partitioned tables | OK | Literal |
| §3 · «el error de la documentación es más corto y no nombra la columna» | Querying partitioned tables | OK | La doc dice: «Cannot query over table 'project_id.dataset.table' without a filter that can be used for partition elimination» — sin `over column(s) 'fecha'` |
| §4 · *"When query results are retrieved from a cached results table, the job statistics property `statistics.query.cacheHit` returns as true, and you are not charged for the query"* | [Cached results](https://docs.cloud.google.com/bigquery/docs/cached-results) | OK | Literal. `Last updated 2026-09-03 UTC` |
| §4 · «`--nouse_cache` (o `useQueryCache = false` en la API)» | Cached results | OK | «Use the nouse_cache flag…» y «set the useQueryCache property to false in the query job configuration» |
| §4 · *"a minimum 10 MB data processed per table referenced by the query, and with a minimum 10 MB data processed per query"* | Pricing | OK | Literal (en la doc va precedido de «Charges are rounded up to the nearest MB, with…») |
| §4 · *(inferencia)* en tabla pequeña todas las consultas facturan igual | — | OK | Se sigue del mínimo y se confirma en los datos: [1], [5] y [6] facturan los tres 10.485.760 |
| §4 · el dry run *"is an upper bound"* | [Cost controls](https://docs.cloud.google.com/bigquery/docs/best-practices-costs) | OK | «The estimate of the number of bytes that is billed for a query is an upper bound». `Last updated 2026-09-03 UTC` |
| §4 · con clustering *"you don't receive an accurate query cost estimate before query execution"* | [Clustered tables](https://docs.cloud.google.com/bigquery/docs/clustered-tables) | OK | Literal |
| §5 · *"a clustered column … sorts storage blocks based on the values in the clustered columns. The storage blocks are adaptively sized based on the size of the table. Colocation occurs at the level of the storage blocks, and not at the level of individual rows"* | Clustered tables | OK | Literal y contiguo en la página. `Last updated 2026-09-03 UTC` |
| §5 · *(inferencia)* si la partición cabe en un bloque no hay nada que saltarse | Clustered tables | OK | Deducción razonable y respaldada por «Only the scanned blocks are used to calculate the bytes of data processed». La medición (0 % a 1 partición, 3,7 % a 90) es consistente |
| §5 · ⚠️ «ninguna página fija un tamaño mínimo a partir del cual el clustering compense» | Partitioned tables, Clustered tables | OK — el ⚠️ está bien puesto | No encontré ningún umbral de ese tipo en ninguna de las páginas consultadas |
| §5 · la única guía de tamaño va en sentido contrario: *"approximately less than 10 GB"* | Partitioned tables | OK | Literal, y el contexto es el que dice la lección: lista «Consider clustering a table **instead of** partitioning a table in the following circumstances: … Partitioning results in a small amount of data per partition (approximately less than 10 GB)» |
| §6 · «BigQuery cobra por las columnas que lee» | Pricing / Clustered tables | OK | «BigQuery uses a columnar data structure…» (Pricing); el laboratorio iguala columnas en `04`/`05` a propósito |
| §7 · *"By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery"* atribuido a **Batch loading** | [Batch loading](https://docs.cloud.google.com/bigquery/docs/batch-loading-data) + Pricing | **Incorrecta (atribución)** | La frase **no aparece** en batch-loading-data (0 coincidencias de «not charged» / «free of charge» en toda la página). Es literal de **Pricing**. El hecho es cierto |
| §7 · *"either all records get inserted or none do"* | Batch loading | OK | Literal: «The result of a BigQuery load job is atomic; either all records get inserted or none do». `Last updated 2026-09-03 UTC` |
| §7 · «El límite de 100 MB … es el de la consola, no el de `bq`» | Batch loading | OK | «When using the Google Cloud console, files loaded from a local data source cannot exceed 100 MB. For larger files, load the file from Cloud Storage.» Confirmado además por la carga real de 172,8 MB con `bq load` |
| §7 · `bq add-iam-policy-binding` *"does not support datasets"* atribuido a **Control access** | [Control access](https://docs.cloud.google.com/bigquery/docs/control-access-to-resources-iam) + [bq CLI reference](https://docs.cloud.google.com/bigquery/docs/reference/bq-cli-reference) | **Incorrecta (atribución)** | La frase **no aparece** en control-access-to-resources-iam. Es literal de la referencia del CLI: «Note: bq add-iam-policy-binding does not support datasets. To modify access to a dataset, see Grant access to a dataset.» (`Last updated 2026-09-03 UTC`). El hecho es cierto |
| §7 · «`bq update --source` sobrescribe los permisos que ya hubiera» | Control access | OK | Sí está en la página citada: «When you apply the JSON file that contains the access controls, the existing access controls are overwritten» |
| §7 · `GRANT … ON SCHEMA` es la vía para un solo dataset | Control access + DCL + estado real | OK | El `GRANT` de `03_permiso_dataset.sql` está efectivamente aplicado (el verificador del laboratorio lo confirmó vía `INFORMATION_SCHEMA.OBJECT_PRIVILEGES`) |
| §8 · *"A partition's expiration time is calculated from the partition boundary in UTC"* | [Managing partitioned tables](https://docs.cloud.google.com/bigquery/docs/managing-partitioned-tables) | OK | Literal. `Last updated 2026-09-03 UTC` |
| §8 · *"Existing partitions expire immediately if they are older than the new expiration time"* | Managing partitioned tables | OK | Literal |
| §8 · «datos de junio a agosto cargados en septiembre se habrían borrado con 90 días» | Managing partitioned tables + manifiesto | OK | Particiones `2026-06-01`…`2026-08-29`; CSV con fecha de carga 2026-09-11. La inferencia se sigue de las dos citas |
| Nombres de servicio 2026 | Pricing (menciona «Gemini Enterprise Agent Platform») | OK | La lección no usa ningún nombre renombrado ni obsoleto (no cita Composer, Dataproc, Vertex AI, Dataplex ni Vector Search) |
| Fechas «act. 2026-09-03» de las 9 páginas etiquetadas | cada página | OK | Editions, Partitioned, Querying partitioned, Managing partitioned, Clustered, Cached, Cost controls, Batch loading y Control access: las nueve dicen `Last updated 2026-09-03 UTC` |
| Enlaces de la sección **Fuentes** | HTTP | OK | Los 12 resuelven (200). Quotas = `Last updated 2026-09-09`, DDL = `2026-09-10` (la lección no les asigna fecha en el cuerpo) |

### C. Formato y nivel (criterio 6)

| Comprobación | Resultado | Evidencia |
|---|---|---|
| Quiz con callouts plegables | OK | Las 8 preguntas usan `> [!question]-` |
| Coherencia quiz ↔ cuerpo | OK | Cada respuesta se corresponde con §3, §4, §5, §6, §7, §8 y §2; ninguna introduce un hecho que no esté en el cuerpo |
| Nivel senior | OK | No explica qué es una partición, un byte ni un índice; explica el mecanismo de BigQuery (bloques de almacenamiento, `statistics.query`, mínimo facturable, semántica de `require_partition_filter`) |
| Aviso de borrador | Coherente | El callout inicial dice que falta verificar la lección contra la documentación: es exactamente este informe |

## Hallazgos

| Gravedad | Dónde (sección) | Qué | Evidencia |
|---|---|---|---|
| **Media** | §7, primer bullet | La cita *"By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery"* se atribuye a [Batch loading](https://docs.cloud.google.com/bigquery/docs/batch-loading-data), donde no aparece. La frase es literal de la página de **Pricing**. El hecho es correcto; el enlace no lo sostiene | Búsqueda de «not charged» y «free of charge» sobre el texto completo de batch-loading-data: **0 coincidencias**. En pricing: «Pricing details By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery.» |
| **Media** | §7, tercer bullet | La cita *"does not support datasets"* se atribuye a [Control access](https://docs.cloud.google.com/bigquery/docs/control-access-to-resources-iam), donde no aparece. Es literal de la **referencia del bq CLI**. El hecho es correcto; el enlace no lo sostiene (la otra mitad del bullet, la de `bq update --source`, sí está en la página citada) | «support datasets» en control-access-to-resources-iam: **0 coincidencias**. En `/bigquery/docs/reference/bq-cli-reference`: «Note: bq add-iam-policy-binding does not support datasets. To modify access to a dataset, see Grant access to a dataset.» |
| Baja | §2, tabla, columna «Capacidad (editions)» | La cita *"compute capacity … measured in slots (virtual CPUs) over time"* no está en [Editions](https://docs.cloud.google.com/bigquery/docs/editions-intro) sino en **Pricing**. La cabecera de §2 cita ambas páginas juntas, así que el lector puede llegar a la buena, pero la columna apunta a Editions | «virtual CPU» y «measured in slots» en editions-intro: 0 coincidencias. En pricing, literal |
| Baja | §5, tabla de clustering | «1 partición (**1,5 MB**)» está en MiB (1.572.459 B = 1,50 MiB), mientras «163 MB» (§1) y «112 MB» (§5) son MB decimales. Se mezclan las dos unidades sin avisar | 1.572.459 / 1.048.576 = 1,4996; / 1.000.000 = 1,57 |
| Baja | §4 | La lección dice que 10.485.760 «son 10 **MiB** exactos» y lo respalda con una cita que dice «10 **MB**». La observación es correcta, pero la discrepancia MB/MiB de la documentación no se señala | Pricing: «a minimum 10 MB data processed per query»; medido: 10.485.760 B = 10 MiB |
| Informativo | §7, segundo bullet | «1 min 19 s» de carga no tiene artefacto en `labs/paso-2/`: el único respaldo es `docs/progreso.md:210`. Entra en la excepción del criterio 4 (dato observado en el laboratorio), pero no es reproducible | `grep -rn "1 min 19"` → solo `docs/progreso.md` |
| Informativo | §5 | El hallazgo del clustering que el verificador del laboratorio dejó explícitamente **sin reproducir** (fuera de su encargo) queda ahora reproducido y confirmado, y la lección lo cuenta con los números correctos | `vl2_…_p5/p6/c1/c2`, `--nouse_cache`, `cacheHit=false` en las cuatro |

Ningún hallazgo es de gravedad alta: no encontré ninguna afirmación técnica falsa, ningún número
que no cuadre con el laboratorio, ninguna cita inventada (las tres mal atribuidas existen, literales,
en otra página oficial) ni ningún nombre de servicio obsoleto.

## Lo que no pude comprobar

- ⚠️ **Los 1 min 19 s de la carga y el `exit 0`.** No hay log de job ni artefacto en `labs/paso-2/`;
  solo la anotación del propio autor en `docs/progreso.md`. Reproducirlo exigiría volver a cargar
  el CSV, que es una escritura y está prohibido en este encargo.
- ⚠️ **Que `bq` no documente en ningún sitio la impersonación por variable de entorno.** Confirmo lo
  que la lección afirma y marca con ⚠️: en `bq --help` (bq 2.1.29) no aparece ningún flag de
  impersonación. No he barrido toda la documentación del CLI para demostrar la negativa completa;
  la lección ya lo marca como observado, no documentado.
- ⚠️ **Que el precio de $6.25/TiB sea idéntico para la multirregión EU.** La página de Pricing sirve
  una sola tabla «Queries (on-demand)» con un selector de región que incluye tanto `US (us)` como
  `Europe (eu)`, y el precio servido en el HTML estático es $6.25. No pude forzar la vista por
  región para ver un eventual precio distinto de la EU; nada en la página sugiere que lo haya.
- ⚠️ **La reproducción del error de `require_partition_filter`.** No relancé las dos consultas que
  fallan: el informe del laboratorio (`2-2026-09-12.md`, comprobaciones 5 y 5b) ya las reprodujo con
  jobs propios y obtuvo el mensaje literal que cita la lección. Me apoyo en esa reproducción.
