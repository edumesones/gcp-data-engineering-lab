# Verificación — Paso 2 (lección, v4) · 2026-09-12

## Veredicto

**PASA**

Las 24 citas literales aparecen en la página que enlazan, con el mismo sentido y en su contexto. Los
12 enlaces devuelven 200 sin redirección. Todas las cifras medidas se reprodujeron con jobs propios y
`--nouse_cache`, salvo el par de 90 particiones del clustering, que excede el presupuesto de lectura del
encargo y coincide exactamente con la evidencia del laboratorio. No hay hallazgos de gravedad media ni
alta; los cinco que se listan son de gravedad baja o informativos.

## Criterio de aceptación

1. **Cada afirmación técnica sobre Google Cloud** (comportamiento de un servicio, límite, precio, estado GA o Preview, nombre, rol, permiso o mensaje de error) es correcta según la **documentación oficial vigente** y lleva enlace a esa documentación.
2. **Las citas literales en inglés** (entre comillas y en cursiva) aparecen tal cual, o con el mismo sentido, **en la página que enlazan** (no en otra página del mismo producto), y se usan en su contexto.
3. Lo que el texto marca como *(inferencia)* es una deducción razonable y no contradice la documentación. Lo que marca con ⚠️ está de verdad sin confirmar, o bien puedes confirmarlo o refutarlo con fuente.
4. Nada se afirma como hecho sin fuente, salvo lo marcado como medido u observado en el laboratorio.
5. **Los números medidos coinciden con la evidencia del laboratorio**, **cada cifra dice de qué magnitud es** (bytes procesados por una consulta, tamaño lógico de la tabla, etc.) y las **unidades son coherentes** (MB decimales frente a MiB binarios).
6. **Cada enlace de la sección Fuentes respalda algo del cuerpo**, y ninguna URL citada redirige a otra página distinta.
7. Formato: quiz con callouts plegables `> [!question]-`, coherente con el cuerpo. Nivel senior: no explica conceptos genéricos de ingeniería, explica lo específico de BigQuery.

## Método

- **Enlaces**: `curl -sL -w '%{url_effective} %{http_code} %{num_redirects}'` sobre las 12 URL citadas, y
  descarga del HTML a texto plano para buscar las citas (WebFetch resume y trunca: no se usó).
- **Citas**: comparación exacta de la cita normalizada en espacios contra el texto de **cada una** de las 12
  páginas, para detectar citas que estén en otra página del mismo producto. Resultado: cada cita aparece en
  la página que enlaza.
- **Mediciones**: jobs propios con `--job_id` propio y `--nouse_cache`, leyendo
  `statistics.query` con `bq show -j`. Configuración `sdag-lab`, cuenta `you@example.com`,
  proyecto `sdag-lab-000000`. Solo lectura: `bq show`, `SELECT`, `INFORMATION_SCHEMA`. No se creó, modificó
  ni borró nada, y no se tocó la configuración `default`.
- **Precio por región**: el HTML estático de la página de precios solo trae la fila de la región
  seleccionada, así que la tabla de on-demand se leyó en un navegador real cambiando el selector de región
  a `Europe (eu)` y a `US (us)`.

## Comprobaciones ejecutadas

### Enlaces (criterio 6)

| URL citada | Código | Redirección | Última actualización de la página | Lo que dice la lección |
|---|---|---|---|---|
| `cloud.google.com/bigquery/pricing` | 200 | **ninguna** | la página no publica fecha | la lección no le atribuye fecha ✅ |
| `docs…/bigquery/docs/editions-intro` | 200 | ninguna | 2026-09-03 | sin fecha ✅ |
| `docs…/bigquery/docs/partitioned-tables` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/querying-partitioned-tables` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/managing-partitioned-tables` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/clustered-tables` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/cached-results` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/best-practices-costs` | 200 | ninguna | 2026-09-03 | sin fecha ✅ |
| `docs…/bigquery/docs/batch-loading-data` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/control-access-to-resources-iam` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/bigquery/docs/reference/bq-cli-reference` | 200 | ninguna | 2026-09-03 | «act. 2026-09-03» ✅ |
| `docs…/sdk/gcloud/reference/config` | 200 | ninguna | 2026-09-09 | sin fecha ✅ |

Ninguna fecha de las que declara la lección es falsa. Nota útil: la forma `docs.cloud.google.com/bigquery/pricing`
**sí** redirige (301) a `cloud.google.com/bigquery/pricing`; la lección cita ya la forma final, que no redirige.

### Cuerpo de la lección

| Afirmación de la lección (sección) | Fuente consultada | Resultado | Evidencia (cita o salida) |
|---|---|---|---|
| §1 1.957.639 filas · 90 particiones · 162,8 MB | `bq show` de la tabla | OK | `"numRows": "1957639"`, `"numPartitions": "90"`, `"numBytes": "162765153"` (= 162,77 MB decimales) |
| §1 `PARTITION BY fecha` · `CLUSTER BY cuenta_id, tipo` · `require_partition_filter = TRUE` | `bq show` | OK | `timePartitioning {field: fecha, type: DAY, requirePartitionFilter: true}`, `clustering {fields:[cuenta_id, tipo]}` |
| §1 Dataset `banca` en multirregión US | `bq show` del dataset | OK | `"location": "US"` |
| §1 «la creó y la cargó la cuenta de servicio del pipeline, impersonada» | `INFORMATION_SCHEMA.JOBS_BY_PROJECT` | OK | job `LOAD` con `user_email = sdag-pipeline@sdag-lab-000000.iam.gserviceaccount.com`; 21 jobs `QUERY` con esa misma identidad |
| §2 *"the number of bytes processed by each query"* | Pricing | OK | literal en Pricing, en «On-demand pricing (per TiB)» |
| §2 «El primer TiB al mes es gratis» | Pricing | OK | *"The first 1 TiB of query data processed per month is free."* |
| §2 *"compute capacity … measured in slots (virtual CPUs) over time"* | Pricing | OK | *"you are charged for compute capacity used to run queries, measured in slots (virtual CPUs) over time"*; la elipsis no altera el sentido |
| §2 *"up to 2,000 concurrent slots, shared among all queries in a single project"* | Pricing | OK la cita, **imprecisa** la etiqueta «Techo» | la cita es literal; la misma frase sigue: *"Periodically, BigQuery will temporarily burst beyond this limit"* → hallazgo 2 |
| §2 «$6.25 por TiB en las multirregiones US y EU» | Pricing, tabla on-demand leída por región en navegador | OK | con el selector en `Europe (eu)`: «1 tebibyte and above 6,25 US$ / 1 tebibyte»; con el selector en `US (us)`: idéntico |
| §2 «Las tres citas de la tabla son de Pricing» | las 12 páginas | OK | las tres aparecen en Pricing y la comprobación cruzada no las encuentra en ninguna otra de las 12 |
| §2 *"You can choose to use editions and the on-demand model at the same time on a per-project basis"* | Editions | OK | literal en editions-intro, junto a la descripción de los tres editions |
| §2 *(inferencia)* «en capacidad roba slots» | — | OK | deducción razonable del modelo de slots; no contradice nada de la documentación |
| §3 1 día → 1 partición, 966.472 procesados, 10.485.760 facturados | job propio `vlec2_…_dia` | OK | `parts=1 proc=966472 billed=10485760 cache=False` |
| §3 90 días → 90 particiones, 68.515.449 procesados, 69.206.016 facturados | job propio `vlec2_…_rango` | OK | `parts=90 proc=68515449 billed=69206016 cache=False` |
| §3 «Un día lee el 1,41 %» | aritmética sobre lo anterior | OK | 966.472 / 68.515.449 = **1,4106 %** |
| §3 *"If a query uses a qualifying filter … skip the remaining partitions"* | Partitioned tables | OK | literal; el párrafo sigue *"This process is called pruning"* |
| §3 Ejemplos que **no** podan: subconsulta, columna contra columna, `OR`, sumar intervalo, `EXTRACT(MONTH …)`, `FORMAT_DATE` | Querying partitioned tables | OK el contenido, **mal atribuida** la página | los seis ejemplos están en *Querying partitioned tables* (subconsulta `(SELECT timestamp FROM table3…)`, `WHERE ts >= ts2`, *"If AND is changed to OR, partition pruning won't work"*, `WHERE ts + INTERVAL 1 DAY > CURRENT_TIMESTAMP()`, `WHERE EXTRACT(MONTH FROM ts) = 3`, `WHERE FORMAT_DATE('%Y-%m-%d %H', ts)`). En *Partitioned tables* **no** hay ninguno → hallazgo 1 |
| §3 «Solo poda un puñado de funciones de fecha» | Querying partitioned tables | OK | la página lista 11: `DATE_ADD`, `DATE_DIFF`, `DATE_SUB`, `DATE_TRUNC`, `DATETIME_DIFF`, `TIMESTAMP_ADD/DIFF/SUB/TRUNC`, `EXTRACT` con partes `DATE` o `YEAR`, `FORMAT_TIMESTAMP` con `%F`, `%Y-%m-%d`, `%Y%m%d` |
| §3 *"Other functions and complex mathematical operations will require a full table scan."* | Querying partitioned tables | OK | literal, cerrando esa misma lista de funciones |
| §3 `EXTRACT(MONTH FROM fecha) = 7` falla con `require_partition_filter` | `resultados_poda.json` consulta [4] | OK | error idéntico al de la consulta sin filtro; el verificador del laboratorio lo reprodujo (`5b`) |
| §3 *"at least one predicate that only references a partitioning column … eligible for partition elimination"* | Querying partitioned tables | OK | literal, en «Require a partition filter in queries», con los ejemplos `WHERE partition_id = "20221231"` válidos y el `OR` inválido |
| §3 «el error real nombra la columna; el de la documentación es más corto» | Querying partitioned tables + jobs del laboratorio | OK | doc: *"Cannot query over table 'project_id.dataset.table' without a filter that can be used for partition elimination."* · real: `… without a filter over column(s) 'fecha' that can be used for partition elimination` |
| §4 La caché devuelve 0 bytes; *"When query results are retrieved from a cached results table … you are not charged for the query"* | Cached results | OK | literal en «Pricing and quotas» de cached-results (el HTML inserta un espacio antes de la coma: *"returns as true , and"*); no aparece en ninguna de las otras 11 páginas |
| §4 «`useQueryCache = false` en la API» | Cached results | OK | la página documenta `useQueryCache` (4 menciones) |
| §4 *"a minimum 10 MB data processed per table referenced by the query, and with a minimum 10 MB data processed per query"* | Pricing | OK | literal, precedido de *"Charges are rounded up to the nearest MB"* |
| §4 «la página dice 10 MB, pero lo facturado son 10.485.760 bytes, es decir 10 MiB» | aritmética | OK | 10.485.760 / 2²⁰ = **10,0 MiB** = 10,49 MB decimales. La distinción de unidades es correcta y está explicitada |
| §4 *(inferencia)* «en una tabla pequeña todas las consultas facturan lo mismo» | jobs propios | OK | 966.472 y 1.572.459 bytes procesados facturan los dos 10.485.760 |
| §4 dry run *"is an upper bound"* | Estimate and control costs | OK | *"The estimate of the number of bytes that is billed for a query is an upper bound, and can be higher than the actual number of bytes billed after running the query."* |
| §4 *"you don't receive an accurate query cost estimate before query execution"* | Clustered tables | OK | literal, precedido de *"When you query a clustered table"* |
| §5 1 partición: 1.572.459 sin filtro y 1.572.459 con `cuenta_id`, **0 %** | jobs propios `vlec2_…_clu5` y `_clu6` | OK | los dos `proc=1572459 billed=10485760 cache=False`: ni un byte de diferencia |
| §5 90 particiones: 111.585.423 → 107.431.263, **3,7 %** | `resultados_clustering.json` | OK contra la evidencia del laboratorio; **no reproducido** por mí | las cifras del fichero son exactamente esas; 1 − 107.431.263/111.585.423 = **3,72 %**. Reproducirlo costaba ~219 MB de lectura, por encima del presupuesto del encargo → hallazgo 5 |
| §5 «son `totalBytesProcessed` de consultas que leen cuatro columnas, no el tamaño de la tabla» | `sql/05_clustering_escala.sql` | OK | las dos consultas piden `transaccion_id, cuenta_id, ts_operacion, importe`: mismas columnas, como exige la comparación |
| §5 «La tabla entera ocupa 162.765.153 bytes (162,8 MB)» | `bq show` | OK | `numBytes = numTotalLogicalBytes = 162765153`; 162,77 MB decimales. La magnitud está dicha |
| §5 «una partición ronda 1,8 MB de media» | aritmética | OK | 162.765.153 / 90 = 1.808.502 bytes = **1,81 MB** |
| §5 *"a clustered column … Colocation occurs at the level of the storage blocks, and not at the level of individual rows"* | Clustered tables | OK | literal en el primer párrafo: *"a clustered column is a user-defined table property that sorts storage blocks based on the values in the clustered columns. The storage blocks are adaptively sized based on the size of the table."* La elipsis solo sustituye *"is a user-defined table property that"* |
| §5 *(inferencia)* «si una partición cabe en un bloque, no hay bloques que saltarse» | Clustered tables | OK | coherente con *"adaptively sized based on the size of the table"*; no contradice nada |
| §5 ⚠️ «ninguna página fija un tamaño mínimo a partir del cual el clustering compense» | las 12 páginas | **⚠️ confirmado como tal** | no aparece ninguna guía de tamaño mínimo para clustering. La única guía de tamaño va, como dice la lección, en sentido contrario |
| §5 *"approximately less than 10 GB"* | Partitioned tables | OK | literal, dentro de *"Consider clustering a table instead of partitioning a table in the following circumstances: … Partitioning results in a small amount of data per partition (approximately less than 10 GB)"*. La lección lo caracteriza bien: clustering **en lugar de** particionado |
| §6 «mi primera comparación pedía 5 columnas en una y 3 en la otra» | narrativa de laboratorio | OK (observado) | consistente con el comentario de `sql/04_consultas_poda.sql`: *«`cuenta_id` va en el SELECT de las dos consultas a propósito»* |
| §7 *"By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery"* | Pricing | OK | literal |
| §7 *"either all records get inserted or none do"* | Batch loading | OK | literal: *"The result of a BigQuery load job is atomic; either all records get inserted or none do."* |
| §7 «El CSV de 172,8 MB» | fichero del laboratorio | OK | `transacciones.csv` = 172.802.717 bytes = **172,8 MB** decimales |
| §7 «el job de carga duró 7,9 s» | `INFORMATION_SCHEMA.JOBS_BY_PROJECT` | OK | único job `LOAD`: `end_time − start_time = 7943 ms` |
| §7 «De 1 min 19 s totales» | — | **sin confirmar** | ningún artefacto de `labs/paso-2/` registra el tiempo de pared del `bq load` → hallazgo 3 |
| §7 «El límite de 100 MB … es el de la consola, no el de `bq`» | Batch loading | OK | *"When using the Google Cloud console, files loaded from a local data source cannot exceed 100 MB. For larger files, load the file from Cloud Storage."* La página no impone ese límite a `bq`, y el laboratorio cargó 172,8 MB con `bq` |
| §7 `GRANT … ON SCHEMA` como vía para un solo dataset | bq CLI reference + Control access | OK | *"Note: bq add-iam-policy-binding does not support datasets. To modify access to a dataset, see Grant access to a dataset."* y *"Caution: When you apply the JSON file that contains the access controls, the existing access controls are overwritten. bq update --source …"* |
| §7 «el ACL del dataset lo muestra como `"role": "WRITER"`» | `bq show` del dataset | OK (observado y confirmado) | `{"role": "WRITER", "userByEmail": "sdag-pipeline@sdag-lab-000000.iam.gserviceaccount.com"}` |
| §7 «`bq --help` (v2.1.29) no ofrece ninguna opción de impersonación» | `bq version`, `bq --help` | OK | `This is BigQuery CLI 2.1.29`; `bq --help | grep -c -i impersonat` → **0** |
| §7 propiedad `auth/impersonate_service_account` documentada | bq CLI reference | OK | *"gcloud config set auth/impersonate_service_account SERVICE_ACCOUNT_NAME … bq commands that you run now use the service account credentials"* |
| §7 «cada propiedad tiene su variable `CLOUDSDK_SECCION_PROPIEDAD`» | gcloud config | OK | *"The matching environment variable for a property is of the form 'CLOUDSDK_CATEGORY_PROPERTY'"* |
| §7 «De ahí sale `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT`» | — | OK, no marcado como inferencia | derivación explícita de dos hechos documentados, y el laboratorio la usó con éxito (`ejecutar_y_medir.py`; los jobs corren como la SA) → hallazgo 4 |
| §8 *"A partition's expiration time is calculated from the partition boundary in UTC"* | Managing partitioned tables | OK | literal |
| §8 *"Existing partitions expire immediately if they are older than the new expiration time"* | Managing partitioned tables | OK | literal |
| §8 «datos de junio a agosto, cargados en septiembre» | `data/manifiesto.json`, jobs | OK | manifiesto: 90 días de `2026-06-01` a `2026-08-29`, total 1.957.639; el job `LOAD` es del 2026-09-12 |
| §8 «La tabla se creó sin expiración» | `bq show` tabla y dataset | OK | sin `expirationMs` en `timePartitioning`; dataset sin `defaultPartitionExpirationMs` ni `defaultTableExpirationMs` |
| §9 «one day scanned 1.4% of the bytes … one partition versus ninety» | jobs propios | OK | 1,41 %, 1 partición frente a 90 |
| §9 «at this data size clustering showed no measurable gain» | jobs propios + ficheros | OK | 0 % en una partición (reproducido), 3,7 % a 90 días (evidencia del laboratorio) |
| Formato: quiz con `> [!question]-` | la propia lección | OK | 8 callouts plegables, todos con la sintaxis pedida; cada respuesta se corresponde con una afirmación del cuerpo |
| Nivel senior | la propia lección | OK | no explica qué es una partición ni un bloque en abstracto; explica poda, `require_partition_filter`, mínimo facturable, caché y bloques de almacenamiento de BigQuery |
| Nombres de servicios (renombrados en 2026) | páginas consultadas | OK | la lección no nombra ningún servicio renombrado; «BigQuery» y «BigQuery editions» son los nombres vigentes en las páginas de hoy |

### Contraste de mis mediciones con los ficheros del laboratorio

| Magnitud | Ficheros del laboratorio | Mi medición independiente | ¿Coincide? |
|---|---|---|---|
| 1 día: particiones / procesados / facturados | 1 / 966.472 / 10.485.760 | 1 / 966.472 / 10.485.760 | Sí, al byte |
| 90 días: particiones / procesados / facturados | 90 / 68.515.449 / 69.206.016 | 90 / 68.515.449 / 69.206.016 | Sí, al byte |
| 1 partición sin filtro de cuenta | 1.572.459 | 1.572.459 | Sí |
| 1 partición con `cuenta_id` | 1.572.459 | 1.572.459 | Sí |
| 90 particiones, clustering | 111.585.423 → 107.431.263 | no reproducido (presupuesto) | — |
| Tamaño lógico de la tabla | 162,8 MB (lección) | `numBytes = 162765153` | Sí |
| Duración del job de carga | 7,9 s (lección) | 7.943 ms | Sí |

Todos mis jobs con `cacheHit = false`.

## Hallazgos

| Gravedad | Dónde (sección) | Qué | Evidencia |
|---|---|---|---|
| **Baja** | §3 «Qué la rompe» | «**La misma página** recoge como ejemplos que no podan…» viene justo después de citar *Partitioned tables*, pero esos ejemplos están en *Querying partitioned tables*. El contenido es correcto y la página correcta se enlaza dos líneas más abajo, en la cita que cierra el párrafo; lo que falla es el antecedente de «la misma página». Se arregla nombrando la página. | Búsqueda de `EXTRACT`, `FORMAT_DATE` y «subquery» en el texto de *Partitioned tables*: **0 coincidencias**. En *Querying partitioned tables* están los seis ejemplos |
| **Baja** | §2, tabla, fila «Techo» | Llamar «Techo» a los 2.000 slots es más fuerte de lo que dice la fuente: la cita es correcta, pero la frase siguiente de la misma página dice que BigQuery los supera temporalmente. «Techo orientativo» o una nota sobre el burst lo dejaría exacto | Pricing: *"you will generally have access to up to 2,000 concurrent slots… Periodically, BigQuery will temporarily burst beyond this limit to accelerate smaller queries."* |
| **Baja** | §7, segundo guion | «De 1 min 19 s totales» no está respaldado por ningún artefacto: es un tiempo de pared que no quedó registrado. La otra mitad del dato (7,9 s) sí se confirma | `INFORMATION_SCHEMA.JOBS_BY_PROJECT` da `ms_job = 7943` para el único job `LOAD`; en `labs/paso-2/` no hay log del `bq load` |
| **Baja** | §7, último guion | `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT` se presenta con «De ahí sale», sin marca *(inferencia)*, aunque es exactamente eso: una composición de dos hechos documentados. Es una deducción correcta y verificada en la práctica, pero la lección se marca las inferencias en otros sitios | `ejecutar_y_medir.py` la usa; los jobs del laboratorio corren como `sdag-pipeline@…`. La variable concreta no aparece literalmente en ninguna de las páginas citadas |
| Informativo | §5, tabla de 90 particiones | Las dos cifras de 90 particiones (111.585.423 y 107.431.263) no las he reproducido: repetir ese par cuesta ~219 MB de lectura, por encima del «unos 120 MB como mucho» del encargo. Coinciden **exactamente** con `resultados_clustering.json`, que es lo que pide el criterio 5 | `resultados_clustering.json`; mi presupuesto se gastó en los cuatro jobs reproducidos (~72 MB) |

Ninguno de estos hallazgos es de gravedad media o alta, así que el veredicto es PASA. Los dos ⚠️ que la
lección declara (tamaño mínimo para que el clustering compense, y el clustering que «no se puede demostrar»
en este laboratorio) son honestos: el primero lo he confirmado como no documentado en las páginas citadas;
el segundo es precisamente lo que muestran las mediciones.

## Lo que no pude comprobar

- ⚠️ **El par de 90 particiones del clustering** (111.585.423 → 107.431.263). Motivo: reproducirlo son
  ~219 MB de lectura y el encargo fija ~120 MB. Contrastado contra la evidencia del laboratorio, no medido
  por mí.
- ⚠️ **«De 1 min 19 s totales»** en §7. Motivo: no hay artefacto que registre el tiempo de pared del
  `bq load`; solo la duración del job (7,9 s), que sí confirmo.
- ⚠️ **El precio por TiB de las regiones distintas de `us` y `eu`.** Motivo: el HTML estático de la página
  de precios solo trae la fila de la región seleccionada y el orden del payload no se corresponde con el de
  las pestañas. Comprobé en navegador las dos que afirma la lección (`Europe (eu)` y `US (us)`: 6,25 US$ / 1
  TiB en las dos) y no hizo falta más.
- ⚠️ **Que los precios y las páginas sigan diciendo esto mañana.** Compruebo el estado de hoy, 2026-09-12,
  con las fechas de actualización anotadas arriba.
