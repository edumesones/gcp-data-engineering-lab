# Verificación — Paso 3 (lección, v2) · 2026-09-12

## Veredicto
PASA

Las 28 citas literales son **verbatim**, las 14 URL citadas devuelven **200 sin una sola redirección**, las
fechas de «Last updated» de las 13 páginas coinciden **exactamente** con las que declara la lección, y los 14
números medidos coinciden con `bq show -j` de los tres jobs y con los ficheros de evidencia. No hay hallazgos
de gravedad media ni alta. Los cuatro hallazgos son de gravedad **baja** y ninguno afecta a una afirmación
técnica del cuerpo: son un matiz que la lección enuncia bien en §2 y pierde en el resumen, un nombre de
servicio sin enlace, un enlace citado en el cuerpo que falta en Fuentes, y un ⚠️ honesto que se puede
**reforzar** con fuente.

## Criterio de aceptación

1. **Cada afirmación técnica sobre Google Cloud** (comportamiento de un servicio, límite, precio, estado GA o Preview, nombre, rol, permiso o mensaje de error) es correcta según la **documentación oficial vigente** y lleva enlace a esa documentación.
2. **Las citas literales en inglés** (entre comillas y en cursiva) aparecen tal cual, **completas** (no cortadas a mitad de frase de forma que cambien el sentido), **en la página que enlazan** (no en otra página del mismo producto), y se usan en su contexto.
3. Lo que el texto marca como *(inferencia)* es una deducción razonable y no contradice la documentación. Lo que marca con ⚠️ está de verdad sin confirmar, o bien puedes confirmarlo o refutarlo con fuente.
4. Nada se afirma como hecho sin fuente, salvo lo marcado como medido u observado en el laboratorio.
5. **Los números medidos coinciden con la evidencia del laboratorio**, **cada cifra dice de qué magnitud es** y las **unidades son coherentes** (MB decimales frente a MiB binarios).
6. **Cada enlace de la sección Fuentes respalda algo del cuerpo**, y ninguna URL citada redirige a otra página distinta.
7. **Las recetas y comandos que propone la lección son aplicables al pipeline que describe**: si una opción solo funciona en ciertos modos de escritura o en ciertos tipos de tabla, la lección lo dice.
8. Formato: quiz con callouts plegables `> [!question]-`, coherente con el cuerpo. Nivel senior: no explica conceptos genéricos de ingeniería, explica lo específico de BigQuery.

## Método

Las 14 páginas se descargaron con `curl -sSL -w "%{http_code} %{url_effective} %{num_redirects}"`, se
convirtieron a texto plano quitando etiquetas y se buscó **cada cita como subcadena normalizada** (no con
WebFetch, que resume y trunca). Tres citas dieron falso negativo en el primer pase (E, N, Q1): la única
diferencia eran espacios que introduce el propio `<code>` al quitar etiquetas
(`Iowa ( us-central1 )`, `REPEATED .`, `( UPDATE , DELETE , and MERGE )`). Comprobado el contexto carácter a
carácter: las tres son verbatim.

Los números se tomaron de `bq --location=US show --format=prettyjson -j` de los tres jobs y de `bq show` de las
dos tablas, con la configuración `sdag-lab` cargada mediante `source entorno.sh` en cada invocación. Una
sola consulta de lectura (`--nouse_cache`, con filtro sobre `fecha`). **No** se ejecutó DML, ni cargas, ni se
creó, modificó o borró nada; la configuración `default` no se usó ni se activó.

## Comprobaciones ejecutadas

### Citas literales (todas verbatim, en la página que enlazan y en su contexto)

| Afirmación de la lección (sección) | Fuente consultada | Resultado | Evidencia (cita o salida) |
|---|---|---|---|
| §2 «load jobs can be a less expensive…» | [Loading data](https://docs.cloud.google.com/bigquery/docs/loading-data) | OK | Verbatim, en la tabla «Loading method / Batch load»: *"If your source data changes infrequently, or you don't need continuously updated results, load jobs can be a less expensive, less resource-intensive way to load your data into BigQuery."* |
| §2 load job atómico | [Batch loading](https://docs.cloud.google.com/bigquery/docs/batch-loading-data) | OK | Verbatim: *"The result of a BigQuery load job is atomic; either all records get inserted or none do."* |
| §2 carga por lotes gratis | [Pricing](https://cloud.google.com/bigquery/pricing) | OK | Verbatim, en «Pricing details»: *"By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery."* |
| §2 cargos por transferencia si el bucket está en otra ubicación | Batch loading | OK | Verbatim. La lección la atribuye a «(Batch loading)» y **está en esa página**, no en Pricing |
| §2 bucket de Iowa para dataset en US | [Locations](https://docs.cloud.google.com/bigquery/docs/locations) | OK | Verbatim. Contexto completo y coherente: *"If your BigQuery dataset is in the US multi-region, then the Cloud Storage bucket can be in the Iowa (us-central1) single region, or any dual-region that includes Iowa."* |
| §2 1.500 modificaciones de tabla al día | [Quotas](https://docs.cloud.google.com/bigquery/quotas) | OK | Verbatim, sección «Standard tables»: *"Your project can make up to 1,500 table modifications per table per day."* |
| §2 los load jobs fallidos también cuentan | Quotas | OK | Verbatim, sección «Load jobs»: *"Load jobs, including failed load jobs, count toward the limit on the number of table operations per day for the destination table."* |
| §2 «no acota los MERGE (el DML está excluido)» — **de la misma página** | Quotas | OK | Confirmado en la misma página: *"DML statements are excluded and don't count toward the number of table modifications per day."* |
| §2 «en una tabla particionada por columna… 30.000 modificaciones de partición al día» — **de la misma página** | Quotas | OK | Confirmado en la misma página, sección «Partitioned tables»: «Number of partition modifications per column-partitioned table per day — 30,000 modifications… *DML statements do not count toward the number of partition modifications per day.*» Las dos cifras y las dos exclusiones son correctas |
| §3 columna nueva NULLABLE queda a NULL | [DDL](https://docs.cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language) | OK | Verbatim: *"NULL if the new column was added with NULLABLE mode. This is the default mode."* |
| §3 `expiration_timestamp` documentado para `CREATE TABLE` y la forma relativa en un ejemplo de `CREATE VIEW` | DDL | OK | Exacto tal como lo formula la lección: en `table_option_list` la opción aparece con ejemplo absoluto (`TIMESTAMP "2025-01-01 00:00:00 UTC"`), y `TIMESTAMP_ADD(CURRENT_TIMESTAMP(), INTERVAL 48 HOUR)` aparece en los ejemplos de `CREATE VIEW` (también en `CREATE SNAPSHOT TABLE` y `CREATE TABLE … CLONE`) |
| §3 `WRITE_TRUNCATE` quita las restricciones y usa el esquema del load | [Job (REST)](https://docs.cloud.google.com/bigquery/docs/reference/rest/v2/Job) | OK | Verbatim y en el contexto correcto: es la entrada `writeDisposition` de la **configuración de load** (*"…uses the schema from the load job"*), que es justo de lo que habla la lección |
| §3 `WRITE_TRUNCATE_DATA` conserva restricciones y esquema | Job (REST) | OK | Verbatim; presente tanto en la configuración de load como en la de query |
| §3 el MERGE falla con clave duplicada | [DML](https://docs.cloud.google.com/bigquery/docs/reference/standard-sql/dml-syntax) | OK | Verbatim: *"UPDATE/MERGE must match at most one source row for each target row."* |
| §4 `--job_id` en `bq load` y `bq query` | [bq CLI](https://docs.cloud.google.com/bigquery/docs/reference/bq-cli-reference) | OK | *"Specifies a job identifier for a new job. This flag applies only to commands that create jobs: cp, extract, load, and query."* Cubre los dos comandos que cita la lección |
| §4 `jobs.insert` es idempotente | [Cargar CSV desde Cloud Storage](https://docs.cloud.google.com/bigquery/docs/loading-data-cloud-storage-csv) | OK | Verbatim y completa: *"Calling jobs.insert on a given job ID is idempotent. You can retry as many times as you like on the same job ID, and at most one of those operations will succeed."* |
| §4 no se puede repetir un job con el mismo id | [Managing jobs](https://docs.cloud.google.com/bigquery/docs/managing-jobs) | OK | Verbatim, sección «Repeat jobs» |
| §4 historial de seis meses — «misma página» | Managing jobs | OK | Verbatim y **en la misma página**, sección «List jobs» |
| §4 `--fingerprint_job_id` deriva el id de la configuración | bq CLI | OK | *"To use a job ID that is derived from a fingerprint of the job configuration, set to true. This prevents the same job from running multiple times accidentally."* La glosa de la lección es fiel |
| §5 fórmula `q'` / `t'` y `q' + t'` para MERGE | DML | OK | Las tres piezas verbatim y contiguas en «Partitioned tables». La tabla dice: MERGE *"If there are only INSERT clauses… q'. If there is an UPDATE or DELETE clause in the MERGE statement: q' + t'."* El recorte con «…» no cambia el sentido |
| §5 poda con filtro en subquery / `search_condition` / `merge_condition` | [DML con tablas particionadas](https://docs.cloud.google.com/bigquery/docs/using-dml-with-partitioned-tables) | OK | Verbatim, sección «Pruning partitions when using a MERGE statement» |
| §5 `WHEN NOT MATCHED BY SOURCE` sin filtro → sin poda | DML con tablas particionadas | OK | Verbatim y en contexto: *"This query must scan the entire target table… As a result, no partitions are pruned."* |
| §5 mínimo de 10 MB por tabla referenciada | Pricing | OK | Verbatim: *"Charges are rounded up to the nearest MB, with a minimum 10 MB data processed per table referenced by the query, and with a minimum 10 MB data processed per query."* |
| §5 2 DML mutantes concurrentes, 20 en cola, los siguientes fallan | Quotas | OK | Las tres verbatim y contiguas: «Concurrent mutating DML statements per table — 2», «Queued mutating DML statements per table — 20», *"If you submit additional mutating DML statements for the table, then those statements fail."* |
| §6 el error se define hablando de `WHERE` | [Querying partitioned tables](https://docs.cloud.google.com/bigquery/docs/querying-partitioned-tables) | OK | Verbatim, sección «Require a partition filter in queries» |
| §6 «at least one predicate that only references a partitioning column» | Querying partitioned tables | OK | Verbatim y en la misma página |
| §6 «La página de DML con tablas particionadas **no menciona** `require_partition_filter` en ningún punto» | DML con tablas particionadas | OK | **Confirmado: 0 ocurrencias** de `require_partition_filter` en toda la página. Y a la inversa: 0 ocurrencias de `MERGE` y de `UPDATE` en Querying partitioned tables |
| §7 columnas nuevas deben ser NULLABLE o REPEATED | [Managing table schemas](https://docs.cloud.google.com/bigquery/docs/managing-table-schemas) | OK | Verbatim, sección «Add an empty column» |
| §7 `ALLOW_FIELD_ADDITION`: «allow adding a nullable field to the schema» | Job (REST) | OK | Verbatim |
| §7 los tres casos en que se admite `schemaUpdateOptions` | Job (REST) | OK | Verbatim. **Comprobado el contexto**: el párrafo aparece **dos veces** en la página, con texto idéntico, una bajo `configuration.load.schemaUpdateOptions` y otra bajo `configuration.query.schemaUpdateOptions`. La lección lo usa para un `bq load`, y la versión de load existe y dice lo mismo → **la cita está en su contexto** |

### URLs y fechas

| Comprobación | Resultado |
|---|---|
| Redirecciones de las 14 URL citadas | **200 y `num_redirects=0` en las 14.** Ninguna redirige a otra página |
| Fechas «act. …» declaradas por la lección | Coinciden **todas**: Batch loading 2026-09-03 ✅ · Locations 2026-09-08 ✅ · Quotas 2026-09-09 ✅ · DDL 2026-09-10 ✅ · Job (REST) 2026-09-01 ✅ · DML 2026-09-03 ✅ · bq CLI 2026-09-03 ✅ · CSV desde GCS 2026-09-03 ✅ · Managing jobs 2026-09-03 ✅ · DML con particionadas 2026-09-03 ✅ · Managing table schemas 2026-09-03 ✅ · Querying partitioned 2026-09-03 ✅ |
| Páginas sin fecha declarada | Loading data (la página dice 2026-09-03) y Pricing (no publica «Last updated»). La lección no les atribuye fecha: correcto |
| Cada enlace de Fuentes respalda algo del cuerpo | Sí, los 12. Ver hallazgo B-3 (falta uno que sí se cita en el cuerpo) |

### Números medidos

| Cifra de la lección | Fuente de verdad | Resultado |
|---|---|---|
| §1 «5 objetos, 7,0 MB» | `load.inputFiles 5`, `load.inputFileBytes 7011558`; `gcloud storage ls -l` → «5 objects, 7011558 bytes» | OK. 7 011 558 B = **7,01 MB decimales** (= 6,69 MiB). La lección dice «MB» y la cifra es decimal: **unidad coherente** |
| §1 «57.347 filas» en staging | `load.outputRows 57347`; `bq show` staging `numRows 57347` | OK |
| §1 «expira en 48 h» | staging `creationTime 1789223638039` → `expirationTime 1789396437958` = **172 799 919 ms ≈ 47,99997 h** | OK |
| §1 «57.302 nuevas, 40 correcciones, 5 claves duplicadas» | `manifiesto_incremental.json`: `filas_nuevas 57302`, 40 entradas en `correcciones`, 5 en `duplicados`; `filas_totales 57347` = 57 302 + 40 + 5 | OK |
| §1 `insertedRowCount: 57302` y `updatedRowCount: 40` | `sdag_p3_merge_tanda1_r1` → `dmlStats {insertedRowCount "57302", updatedRowCount "40", deletedRowCount "0"}` | OK, literal |
| §1 «suman 57.342… (57.347 filas, 57.342 ids)» | 57 302 + 40 = 57 342 | OK |
| §1/§3 «1.957.639 → 2.014.941 filas» | `evidencia_antes.json` 1 957 639 · `bq show` tabla final `numRows 2014941` | OK |
| §3 «los 1,96 M de filas anteriores… a NULL» | Consulta propia `--nouse_cache` sobre `fecha BETWEEN 2026-06-01 AND 2026-08-29`: `filas 1957639`, `estado_null 1957599`, `updated_null 1957599` (los 40 no nulos son las correcciones) | OK |
| §3 tabla de la prueba: huellas 9034513496153531947 / 289587634311190219 / 289587634311190219 | Los tres ficheros de evidencia, campo `huella_contenido` | OK, coinciden los tres |
| §3 «Filas afectadas: 57.342 / 0» | r1 `57302+40 = 57342`; r2 `numDmlAffectedRows 0`, `dmlStats` todo a `"0"` | OK |
| §3 ids únicos 1.957.639 / 2.014.941 / 2.014.941 | `ids_unicos` de los tres ficheros de evidencia | OK |
| §5 «Particiones procesadas: 2 → 5» | r1 `totalPartitionsProcessed 2`; r2 `totalPartitionsProcessed 5` | OK |
| §5 «Bytes procesados 9.408.853 / 8.516.138» | r1 `totalBytesProcessed 9408853`; r2 `8516138` | OK |
| §5 «Bytes facturados 20.971.520, las dos veces» | r1 y r2 `totalBytesBilled 20971520` | OK |
| §5 «Aquí hay dos tablas, destino y staging: 10 + 10» | `referencedTables` de ambos MERGE = `transacciones`, `stg_transacciones_tanda1` (exactamente 2) | OK |
| §5 «10.485.760 bytes por tabla, es decir 10 MiB; 20.971.520 son 20 MiB exactos» | 20 971 520 / 1 048 576 = **20,0 exacto**; 10 485 760 / 1 048 576 = 10,0 | OK. La distinción MB decimal (documentación) / MiB binario (facturación) está bien planteada y es la misma observación de la lección 2 |
| §7 «Añadir las dos columnas… es instantáneo» | Job del `ALTER` (`bqjob_r6dad08e6ef70f1e6…`, 14:33:43Z): **182 ms** | OK, la respalda |
| §2 «el bucket de este laboratorio está en `us-central1` y el dataset en la multirregión US» | `gcloud storage buckets describe` → `US-CENTRAL1`, `location_type region`; `bq show banca` → `"location": "US"` | OK |
| §6 «La tabla final tiene `require_partition_filter = TRUE`» | `bq show` → `requirePartitionFilter: true`, `timePartitioning {field: fecha, type: DAY}` | OK |
| §6 «Las dos ejecuciones terminaron sin el error de eliminación de particiones» | r1 y r2: `state DONE`, `errorResult` ausente | OK |
| §3 «`bq load --replace` es `WRITE_TRUNCATE`» | Configuración del load job: `writeDisposition "WRITE_TRUNCATE"` | OK, medido |
| §7 «sobre una tabla sin particionar» (la staging) | `bq show` staging → `timePartitioning: null` | OK |
| §3 «las columnas `estado` y `updated_at` se añadieron en este paso» (NULLABLE) | Esquema: `estado` STRING y `updated_at` TIMESTAMP **sin `mode`** → NULLABLE; las otras 9, `REQUIRED` | OK |

### Inferencias, ⚠️ y formato

| Comprobación | Resultado |
|---|---|
| §2 *(inferencia)* «1.440 load jobs al día, pegado al límite» | Razonable **para el pipeline tal como está montado**: el load apunta a la staging, que es una tabla estándar sin particionar, y ahí el límite aplicable es justo el de 1.500 modificaciones al día. No contradice la documentación. Ver hallazgo B-1 sobre el resumen |
| §3 *(inferencia)* «Sin esa condición, ninguna de las 40 correcciones se habría aplicado» | Correcta. `S.updated_at > NULL` es NULL en GoogleSQL, y una condición NULL no satisface el `WHEN MATCHED`. Medido a favor: las 40 filas del paso 2 con `updated_at` no nulo son exactamente las correcciones aplicadas |
| §4 *(inferencia)* regla del id determinista por intervalo | Deducción razonable; no contradice nada. Se apoya en dos citas verificadas (idempotencia de `jobs.insert` y «no es posible repetir un job con el mismo id») |
| §5 *(inferencia)* «las tres particiones nuevas nacían con el INSERT» | Consistente con lo medido (2 → 5 particiones procesadas) y con `q'`/`t'`: en r1 no existían las particiones nuevas que había que sumar a `t'`. No contradice la documentación |
| §6 ⚠️ «Observado, no documentado» | Honesto y correcto en su parte negativa (verificado: 0 menciones de `require_partition_filter` en la página de DML, 0 de MERGE en la de consultas). **Se puede reforzar con fuente**: ver hallazgo B-4 |
| Criterio 4 — nada como hecho sin fuente | Cumple. Lo no enlazado está marcado como medido («Lo medido», «Comprobado», «Lo probé») o como *(inferencia)*. Única excepción: el nombre «Storage Write API» (hallazgo B-2) |
| Criterio 8 — quiz plegable | **10 de 10** callouts son `> [!question]-`; ninguno sin plegar. Los otros dos callouts (`[!warning] Borrador`, `[!tip] La clave`) son del cuerpo |
| Criterio 8 — coherencia quiz/cuerpo | Las 10 respuestas se corresponden con §1–§7; ninguna introduce un dato que el cuerpo no sostenga, salvo el matiz de B-1 |
| Criterio 8 — nivel senior | Cumple. No explica qué es idempotencia, partición ni `ROW_NUMBER` en abstracto; explica el comportamiento de BigQuery (coste `q'+t'`, `WRITE_TRUNCATE` frente a `WRITE_TRUNCATE_DATA`, mínimo por tabla referenciada, cuotas de DML concurrente) |
| Criterio 7 — aplicabilidad | Cumple bien, y es el punto más cuidado de la lección: §7 avisa de que `ALLOW_FIELD_ADDITION` **no** sirve con `WRITE_TRUNCATE` sobre tabla normal, §3 avisa de que `WRITE_TRUNCATE` pisa el esquema, §5 avisa de que `WHEN NOT MATCHED BY SOURCE` impide la poda, §3 acota `expiration_timestamp` («En este laboratorio funcionó sobre una tabla») |

## Hallazgos

| Gravedad | Dónde (sección) | Qué | Evidencia |
|---|---|---|---|
| **Baja** | «Lo que me llevo» (viñeta 2) y quiz «¿Cuándo dejan de servir los load jobs?» | El resumen y el quiz presentan «1.500 modificaciones por tabla y día» como **el** techo, sin el matiz que §2 sí enuncia. En la documentación ese límite está bajo «Standard tables»; para la tabla **final** de este pipeline, que está particionada por la columna `fecha`, el límite por día que la página asocia a las particionadas es 30.000 modificaciones de partición. El cuerpo (§2) lo dice bien y la inferencia es defendible porque el load apunta a la staging **sin particionar**; solo se pierde precisión al resumir. Sugerencia: en la viñeta y en el quiz, «1.500 por tabla estándar al día (30.000 modificaciones de partición si es particionada por columna)» | Quotas: «Standard tables… Table modifications per day 1,500» frente a «Partitioned tables… Number of partition modifications per column-partitioned table per day 30,000». `bq show` tabla final: `timePartitioning {field: fecha}`; staging: `timePartitioning: null` |
| **Baja** | §2, cierre de la *(inferencia)* | «toca la **Storage Write API**» se afirma sin enlace, y el nombre ya no es único: la documentación vigente distingue **Storage Write API (gRPC)** y **Storage Write API (REST)**, y recomienda la primera para proyectos nuevos. Además, `/bigquery/docs/write-api` **redirige (301)** a `/bigquery/docs/write-api-grpc`. La familia sigue llamándose así, así que no es un nombre caducado, pero conviene enlazarlo y decir cuál | `curl -L` sobre `/bigquery/docs/write-api` → `final=…/write-api-grpc`, `redirects=1`. Título: «Introduction to the Storage Write API (gRPC)». Texto: *"For new projects, we recommend using the Storage Write API (gRPC) instead of the Storage Write API (REST)."* (act. 2026-09-03). `/bigquery/docs/storage-write-api` → **404** |
| **Baja** | Fuentes | La lista tiene 12 enlaces, pero el cuerpo cita **13** páginas: falta [Querying partitioned tables](https://docs.cloud.google.com/bigquery/docs/querying-partitioned-tables), que es la que sostiene las dos citas de §6. El criterio inverso (que cada enlace de Fuentes respalde algo del cuerpo) se cumple en los 12 | Recuento sobre el fichero de la lección: §6 enlaza `querying-partitioned-tables`; el bloque «Fuentes» de las líneas 325-337 no lo incluye |
| **Baja** | §6 y quiz «¿Basta el filtro dentro del `ON`?» | El ⚠️ «no documentado» es honesto, pero **se puede reforzar con fuente** en lugar de dejarlo solo en lo observado: (a) la página de DML con particionadas dice que un filtro en el `merge_condition` sobre la columna de partición **poda particiones**, y (b) la de consultas define el requisito como *"at least one predicate that only references a partitioning column for the filter to be considered eligible for partition elimination"* — es decir, el requisito se formula sobre el **predicado elegible**, y los ejemplos con `WHERE` son eso, ejemplos. `T.fecha BETWEEN …` en el `ON` es un predicado que solo referencia la columna de partición. Sigue sin haber una frase que lo afirme para MERGE, así que el ⚠️ debe quedarse, pero puede pasar de «no documentado» a «no afirmado explícitamente para MERGE, aunque se deduce de dos páginas» | DML con particionadas: *"you can limit which partitions are scanned by including the partitioning column in either a subquery filter, a `search_condition` filter, or a `merge_condition` filter"*. Querying partitioned tables: *"There must be at least one predicate that only references a partitioning column…"*. Recuentos: `require_partition_filter` 0 veces en la primera, `MERGE` 0 veces en la segunda |
| Informativo | §4 | El mensaje `BigQuery error in load operation: Already Exists: Job sdag-lab-000000:US.sdag_p3_load_tanda1` **no lo pude reproducir**: reenviar la carga es una operación de escritura y el encargo la prohíbe. La lección lo presenta como observado en el laboratorio («Lo probé repitiendo la carga con el mismo id»), que el criterio 4 permite. El job existe con ese id exacto y en la región `US`, así que el mensaje es consistente con lo que devolvería `bq` | `bq --location=US show -j sdag_p3_load_tanda1` → `id "sdag-lab-000000:US.sdag_p3_load_tanda1"`. La cita documental que lo explica (`jobs.insert` idempotente) sí está verificada |
| Informativo | §1 y §3 | Las dos unidades que convivían en el paso están bien resueltas y no se contradicen: la lección dice «7,0 MB» (decimales, 7 011 558 B) y el informe del laboratorio decía «6,69 MiB»; son la misma cifra. Nada que corregir | `gcloud storage ls -l` → «5 objects, 7011558 bytes (6.69MiB)» |

## Lo que no pude comprobar

- ⚠️ **El mensaje literal `Already Exists: Job …` de §4.** Requiere reenviar el load job con el mismo `--job_id`, que es una escritura y está prohibida. Queda como observado en el laboratorio (permitido por el criterio 4) y respaldado indirectamente por la cita verificada de `jobs.insert`. Lo que sí comprobé es que el id existe tal y como aparece en el mensaje.
- ⚠️ **Que la primera ejecución solo tuviera dos particiones existentes** (la *(inferencia)* de §5). Es la explicación más razonable del salto 2 → 5 y es coherente con la definición de `t'`, pero el metadato histórico de la tabla en ese instante ya no es recuperable: `bq show -j` informa del número de particiones procesadas, no de cuáles. No la puedo confirmar ni refutar por medición directa; no contradice ninguna fuente.
- ⚠️ **Si el límite de 1.500 «table modifications per day» aplica además a las tablas particionadas.** La página de cuotas separa «Standard tables» y «Partitioned tables», y una tabla particionada es también una tabla estándar (frente a las externas), pero la sección de particionadas **no** repite la entrada de modificaciones de tabla y la de troubleshooting solo documenta el error de las 30.000 modificaciones de partición (0 menciones de «table modifications»). La documentación no zanja si se acumulan los dos límites. Por eso el hallazgo B-1 es de gravedad baja y no media: la cifra que cita la lección es correcta y está en la página; lo único impreciso es presentarla como el único techo al resumir.
- ⚠️ **Fecha de «Last updated» de la página de Pricing.** No publica ese campo. La lección tampoco le atribuye fecha, así que no hay nada que contrastar.
