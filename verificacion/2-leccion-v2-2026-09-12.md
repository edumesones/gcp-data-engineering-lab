# Verificación — Paso 2 (lección, v2) · 2026-09-12

## Veredicto

**PASA CON RESERVAS**

Todas las citas literales existen, literalmente y en su contexto, **en la página que la lección enlaza**.
Todos los números medidos coinciden con la evidencia del laboratorio y con mis propias mediciones
independientes (poda, caché, mínimo facturado, tamaño de la tabla). No hay ningún hallazgo de gravedad alta:
ninguna afirmación técnica es falsa.

Quedan **dos hallazgos de gravedad media**, ambos en la sección 7:

- el límite de 100 MB de ficheros locales se afirma como "lo que documenta Google" **sin enlace**, y la página
  que lo dice no está en la lista de fuentes (la afirmación es correcta: la he confirmado);
- el ⚠️ "`bq` impersona con `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT` … **no documentado**" es refutable: la
  impersonación de `bq` **sí** está documentada en la referencia del `bq` CLI, y el nombre de la variable se
  deriva del patrón de variables de entorno documentado por gcloud. Lo único correcto de ese ⚠️ es que
  `bq --help` no ofrece flag de impersonación (comprobado).

## Criterio de aceptación

1. **Cada afirmación técnica sobre Google Cloud** (comportamiento de un servicio, límite, precio, estado GA o Preview, nombre, rol, permiso o mensaje de error) es correcta según la **documentación oficial vigente** y lleva enlace a esa documentación.
2. **Las citas literales en inglés** (entre comillas y en cursiva) aparecen tal cual, o con el mismo sentido, **en la página que enlazan** (no en otra página del mismo producto), y se usan en su contexto.
3. Lo que el texto marca como *(inferencia)* es una deducción razonable y no contradice la documentación. Lo que marca con ⚠️ está de verdad sin confirmar, o bien puedes confirmarlo o refutarlo con fuente.
4. Nada se afirma como hecho sin fuente, salvo lo marcado como medido u observado en el laboratorio.
5. **Los números medidos coinciden con la evidencia del laboratorio**, y las **unidades son coherentes** (MB decimales frente a MiB binarios).
6. Formato: quiz con callouts plegables `> [!question]-`, coherente con el cuerpo. Nivel senior: no explica conceptos genéricos de ingeniería, explica lo específico de BigQuery.

## Método

- Cada página citada se descargó con `curl -sL` y se convirtió a texto plano; las citas se buscaron **en el
  fichero de esa página concreta**, no en un buscador. Las fechas "act. AAAA-MM-DD" se comprobaron contra la
  línea `Last updated … UTC` del propio HTML.
- Las mediciones se reprodujeron con jobs propios (`--nouse_cache` donde tocaba) sobre
  `sdag-lab-000000.banca.transacciones`, con la configuración `sdag-lab` (`you@example.com`,
  proyecto `sdag-lab-000000`). Solo lectura: `bq show`, `bq show -j`, `SELECT`, `INFORMATION_SCHEMA.JOBS_BY_PROJECT`.
  No se creó, modificó ni borró ningún recurso.
- Nombres de servicio: las páginas descargadas son de 2026 (la de precios habla de *Gemini Enterprise Agent
  Platform*, el nombre vigente de Vertex AI). **BigQuery** conserva su nombre en toda la documentación
  consultada; la lección no menciona ningún servicio renombrado.

## Comprobaciones ejecutadas

### Citas literales (criterio 2)

| Cita de la lección (sección) | Página que la lección enlaza | Resultado | Evidencia |
|---|---|---|---|
| *"the number of bytes processed by each query"* (§2) | cloud.google.com/bigquery/pricing | **OK** | "On-demand pricing (per TiB). With this pricing model, you are charged for **the number of bytes processed by each query**. The first 1 TiB of query data processed per month is free." |
| *"compute capacity … measured in slots (virtual CPUs) over time"* (§2) | pricing | **OK** | "you are charged for **compute capacity** used to run queries, **measured in slots (virtual CPUs) over time**". La elipsis sustituye "used to run queries". |
| *"up to 2,000 concurrent slots, shared among all queries in a single project"* (§2) | pricing | **OK** | "With on-demand pricing, you will generally have access to **up to 2,000 concurrent slots, shared among all queries in a single project**." |
| *"You can choose to use editions and the on-demand model at the same time on a per-project basis."* (§2) | (sin enlace propio; la sección enlaza Pricing y Editions) | **OK, atribución imprecisa** | **No está en Pricing** (0 apariciones). Está literal en `editions-intro`, que sí es una de las dos páginas enlazadas en la sección. Ver hallazgo B-1. |
| *"If a query uses a qualifying filter on the value of the partitioning column, BigQuery can scan the partitions that match the filter and skip the remaining partitions"* (§3) | docs…/partitioned-tables | **OK** | Literal, en el párrafo que sigue con "This process is called pruning." Página: `Last updated 2026-09-03 UTC` ✔ coincide con "act. 2026-09-03". |
| *"Other functions and complex mathematical operations will require a full table scan."* (§3) | docs…/querying-partitioned-tables | **OK** | Literal, cierra la lista de funciones que sí podan (`EXTRACT with DATE or YEAR parts`, etc.). `MONTH` **no** está en esa lista: sostiene el antipatrón del laboratorio. `Last updated 2026-09-03` ✔ |
| *"There must be at least one predicate that only references a partitioning column for the filter to be considered eligible for partition elimination"* (§3) | misma página | **OK (literal); uso imprecisa** | Literal. Pero ese requisito **no explica por sí solo** el fallo de `EXTRACT(MONTH FROM fecha)=7`, que sí referencia únicamente la columna de partición. Ver hallazgo B-2. |
| Mensaje de error de la doc, más corto que el real (§3) | misma página | **OK** | Doc: "Cannot query over table 'project_id.dataset.table' **without a filter that can be used for partition elimination**". Error real reproducido (verificación anterior y `resultados_poda.json`): "…**without a filter over column(s) 'fecha'** that can be used for partition elimination". La observación de la lección es correcta. |
| *"When query results are retrieved from a cached results table, the job statistics property `statistics.query.cacheHit` returns as true, and you are not charged for the query"* (§4) | docs…/cached-results | **OK** | Literal. `Last updated 2026-09-03` ✔ |
| *"a minimum 10 MB data processed per table referenced by the query, and with a minimum 10 MB data processed per query"* (§4) | pricing | **OK** | "Charges are rounded up to the nearest MB, with **a minimum 10 MB data processed per table referenced by the query, and with a minimum 10 MB data processed per query**." |
| *"is an upper bound"* (§4) | docs…/best-practices-costs ("Cost controls") | **OK** | "The estimate of the number of bytes that is billed for a query **is an upper bound**, and can be higher than the actual number of bytes billed after running the query." `Last updated 2026-09-03` ✔ |
| *"you don't receive an accurate query cost estimate before query execution"* (§4) | docs…/clustered-tables | **OK** | "When you query a clustered table, **you don't receive an accurate query cost estimate before query execution** because the number of storage blocks to be scanned is not known before query execution." `Last updated 2026-09-03` ✔ |
| *"a clustered column … sorts storage blocks based on the values in the clustered columns. The storage blocks are adaptively sized based on the size of the table. Colocation occurs at the level of the storage blocks, and not at the level of individual rows"* (§5) | docs…/clustered-tables | **OK** | Literal y seguido, en el primer párrafo. La elipsis sustituye "is a user-defined table property that". |
| *"approximately less than 10 GB"* (§5, ⚠️) | docs…/partitioned-tables | **OK** | En la lista de cuándo usar clustering **en vez de** particionado: "Partitioning results in a small amount of data per partition (**approximately less than 10 GB**)". Uso en contexto correcto. |
| *"By default, you are not charged for batch loading data from Cloud Storage or from local files into BigQuery"* (§7) | pricing | **OK** | Literal, en "Pricing details" de carga de datos. |
| *"either all records get inserted or none do"* (§7) | docs…/batch-loading-data | **OK** | "The result of a BigQuery load job is atomic; **either all records get inserted or none do**." `Last updated 2026-09-03` ✔ |
| *"does not support datasets"* (§7) | docs…/reference/bq-cli-reference | **OK** | "Note: **bq add-iam-policy-binding does not support datasets**. To modify access to a dataset, see Grant access to a dataset." `Last updated 2026-09-03` ✔ |
| "`bq update --source` sobrescribe los permisos que ya hubiera" (§7) | docs…/control-access-to-resources-iam | **OK** | "Caution: When you apply the JSON file that contains the access controls, **the existing access controls are overwritten**." `Last updated 2026-09-03` ✔ |
| *"A partition's expiration time is calculated from the partition boundary in UTC"* (§8) | docs…/managing-partitioned-tables | **OK** | Literal. `Last updated 2026-09-03` ✔ |
| *"Existing partitions expire immediately if they are older than the new expiration time"* (§8) | misma página | **OK** | Literal, en el párrafo sobre actualizar la expiración de una tabla ya creada. Uso en contexto correcto. |

### Afirmaciones técnicas sin cita literal (criterio 1 y 4)

| Afirmación (sección) | Fuente consultada | Resultado | Evidencia |
|---|---|---|---|
| BigQuery separa almacenamiento y cómputo y factura de dos maneras (§2) | pricing | **OK** | "BigQuery pricing has two main components: Compute pricing … Storage pricing". |
| "$6.25 por TiB en las multirregiones US y EU" (§2) | pricing | **OK** | Extraída la tabla por región del HTML: **US (us) → $6.25 / 1 tebibyte** y **Europe (eu) → $6.25 / 1 tebibyte** (de 48 regiones; otras difieren: europe-west1 $7.50, europe-west10 $9.625, me-central2 $10.00). |
| "El primer TiB al mes es gratis" (§2) | pricing | **OK** | "0 tebibyte to 1 tebibyte — Free per 1 month / account"; "The first 1 TiB per month is free." |
| "Editions … Slot-hours, con autoescalado y compromisos" (§2) | editions-intro + pricing | **OK** | "You can use the BigQuery autoscaler or purchase slot commitments…"; editions-intro describe ediciones, reservas y compromisos. `Last updated 2026-09-03` ✔ |
| "BigQuery cobra por las columnas que lee" (§6) | pricing | **OK** | "BigQuery uses a columnar data structure. You're charged according to the total data processed in the columns you select." (la lección lo afirma sin enlace, pero es el mismo Pricing ya enlazado en §2 y §4; gravedad baja) |
| "La poda exige un **filtro constante** sobre la columna de partición" (§3) | querying-partitioned-tables | **OK** | "Use a constant filter expression … filter the partitioning column using a constant expression, rather than a dynamic expression." |
| "No podan las subconsultas" (§3) | misma | **OK** | Ejemplo doc: `WHERE t1.ts = (SELECT timestamp FROM table3 WHERE key = 2)` "is not a constant expression … prevents partition pruning". |
| "…comparar con otra columna" (§3) | misma | **OK** | "predicates don't prune partitions because they require a computation based on a second, non-constant table column ts2 or duration: `WHERE ts >= ts2`". |
| "…la aritmética sobre la columna" (§3) | misma | **imprecisa** | La doc permite envolver la columna en funciones soportadas con argumentos constantes: `WHERE TIMESTAMP_ADD(datehour, INTERVAL 1 DAY) < '2025-01-03'` **sí** poda. Lo que no poda es aislar mal la columna o usar funciones no soportadas. Ver hallazgo B-3. |
| "…un `OR` con otra columna" (§3) | misma | **OK** | "the following is not sufficient, and will result in an error: `WHERE partition_id = "20221231" OR f = "20221130"`". |
| "…ni la mayoría de funciones" (§3) | misma | **OK** | Lista cerrada de funciones que podan + "Other functions … will require a full table scan". |
| "Con `require_partition_filter = TRUE`, una consulta sin filtro de partición falla" (§3) | querying-partitioned-tables + laboratorio | **OK** | Doc + job `sdag_p2_20260912_122214_3` (falla) y el job `verif2_20260912_c_sinfiltro` de la verificación anterior. |
| "`bq load` desde fichero local es gratis" (§7) | pricing | **OK** | Cita verificada arriba. |
| "El límite de 100 MB … es el de la consola, no el de `bq`" (§7) | docs…/loading-data-local | **correcta, pero SIN enlace** | "When using the Google Cloud console, files loaded from a local data source **cannot exceed 100 MB**. For larger files, load the file from Cloud Storage." `Last updated 2026-09-03`. La lección **no enlaza esta página** ni la incluye en Fuentes. Hallazgo A-1. |
| "`GRANT … ON SCHEMA` es la vía para dar un rol sobre un solo dataset" (§7) | bq-cli-reference + control-access-to-resources-iam | **OK** | Las dos citas de apoyo verificadas; el `GRANT … ON SCHEMA` ejecutado está en `sql/03_permiso_dataset.sql`. |
| "`roles/bigquery.dataEditor` aparece en el ACL como `"role": "WRITER"`" (§7) | docs…/access-control-basic-roles | **OK (y además documentado)** | "The BigQuery Data Editor (`roles/bigquery.dataEditor`) predefined IAM role **is mapped to the WRITER** BigQuery basic role." La lección lo presenta como curiosidad observada; es correcto y podría citarse. |
| ⚠️ "`bq` impersona con `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT`. En `bq --help` no aparece ningún flag" (§7) | bq-cli-reference + cloud.google.com/sdk/docs/properties + `bq --help` local | **el ⚠️ es refutable; la 2ª mitad OK** | bq-cli-reference tiene un apartado de impersonación: "To start running bq commands using **service account impersonation**, run: `gcloud config set auth/impersonate_service_account SERVICE_ACCOUNT_NAME`". Y gcloud documenta: "The name of the environment variable follows the **CLOUDSDK_SECTION_NAME_PROPERTY_NAME** pattern" → `auth/impersonate_service_account` = `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT`. Comprobado además: `bq --help` (BigQuery CLI 2.1.29) **no** contiene "impersonat" ✔. Hallazgo A-2. |
| "La expiración … se cuenta desde la fecha de la partición" y "La tabla se creó **sin expiración**" (§8) | managing-partitioned-tables + `bq show` | **OK** | Citas verificadas; `bq show` de la tabla: `timePartitioning` **sin** `expirationMs`. |

### Números medidos y unidades (criterio 5)

| Número de la lección | Evidencia del laboratorio | Mi medición independiente | Resultado |
|---|---|---|---|
| 1.957.639 filas · 90 particiones (§1) | `manifiesto.json` total = 1.957.639 | `bq show`: `numRows 1957639`, `numPartitions 90` | **OK** |
| Tabla de **163 MB** (§1) | — | `bq show`: `numBytes = 162.765.153` → 162,8 MB decimales | **OK** (redondeo correcto) |
| Dataset multirregión US (§1) | — | `bq show` tabla: `"location": "US"` | **OK** |
| "La tabla la creó y la cargó la cuenta de servicio del pipeline, impersonada" (§1) | `resultados_poda.json`: `impersonando: sdag-pipeline@…` | `INFORMATION_SCHEMA.JOBS_BY_PROJECT`: único job `LOAD`, `user_email = sdag-pipeline@sdag-lab-000000.iam.gserviceaccount.com` | **OK** |
| 1 día → 1 partición, 966.472 B procesados, 10.485.760 B facturados (§3, §4) | `resultados_poda.json` [1] | Job propio `ver2lec_cache_a` (`--nouse_cache`): `totalPartitionsProcessed 1`, `totalBytesProcessed 966472`, `totalBytesBilled 10485760`, `cacheHit false` | **OK, reproducido al byte** |
| 90 días → 90 particiones, 68.515.449 B / 69.206.016 B (§3) | `resultados_poda.json` [2] | Reproducido en la verificación previa del paso (mismos valores) | **OK** |
| "Un día lee el **1,41 %**" (§3) y "1.4%" (§9) | — | 966.472 / 68.515.449 = **1,4106 %** | **OK** |
| "10.485.760 bytes, es decir **10 MiB**" mientras la doc dice "10 MB" (§4) | — | 10 × 1024² = 10.485.760 ✔ | **OK**, y es una distinción correcta y bien señalada |
| Caché: "0 bytes procesados y ninguna partición" (§4) | no hay artefacto en `labs/paso-2` (todos los jobs con `desde_cache: false`) | Reproducido: jobs `ver2lec_c1` (`cacheHit false`, 378.996 B) → `ver2lec_c2` y `ver2lec_c3` (`cacheHit true`, `totalBytesProcessed 0`, `totalBytesBilled 0`, **sin** `totalPartitionsProcessed`) | **OK, reproducido** |
| Clustering 1 partición: 1.572.459 vs 1.572.459 → **0 %** (§5) | `resultados_poda.json` [5] y [6], idénticos | — | **OK** |
| Clustering 90 días: 111.585.423 vs 107.431.263 → **3,7 %** (§5) | `resultados_clustering.json` | (111.585.423 − 107.431.263)/111.585.423 = **3,723 %** | **OK** |
| "1,57 MB" y "111,6 MB" como MB decimales (§5) | — | 1.572.459 B = 1,572 MB; 111.585.423 B = 111,59 MB | **OK** (unidades coherentes con lo que declara) |
| "Los tamaños van en MB decimales, como los cuenta BigQuery en `total_logical_bytes`" (§5) | — | `INFORMATION_SCHEMA.TABLE_STORAGE`: `total_logical_bytes` = "Total number of logical (uncompressed) **bytes**". BigQuery no cuenta en MB decimales; además su facturación de almacenamiento va en **GiB** binarios ("1 GiB is 2^30 bytes") | **imprecisa** — hallazgo B-4 |
| "CSV de **172,8 MB**" (§7) | `data/transacciones.csv` = **172.802.717 bytes** = 172,8 MB decimales | — | **OK** |
| "se cargó … en **1 min 19 s**" (§7) | `docs/progreso.md` línea 210: "Carga del CSV de 172,8 MB con `bq load` local … **1 min 19 s, exit 0**" | El job `LOAD` en sí duró **7 s** (12:16:55 → 12:17:03 UTC); el resto es la subida del fichero local, que no entra en la duración del job | **OK como medida de reloj**, registrada en el laboratorio. Se anota el matiz en "Lo que no pude comprobar". |
| Consultas "idénticas salvo el filtro" y "mismas columnas" (§3, §5, §6) | `sql/04_consultas_poda.sql` [1] vs [2] y [5] vs [6]; `sql/05_clustering_escala.sql` [1] vs [2] | — | **OK**: las parejas comparadas tienen SELECT y columnas idénticos; solo cambia el filtro |
| "el generador reparte las cuentas al azar" (§5, inferencia) | `generar_transacciones.py`: `f"CU{rng.randrange(1, n_cuentas + 1):07d}"` sobre 50.000 cuentas | — | **OK** |

### Inferencias y ⚠️ (criterio 3)

| Marca | Dónde | Resultado |
|---|---|---|
| *(inferencia)* "una consulta mal filtrada duele en los dos modelos: en on-demand cuesta dinero, en capacidad roba slots" | §2 | **Razonable**, no contradice la doc: en capacidad se paga por slots reservados y las consultas compiten por ellos. |
| *(inferencia)* "En una tabla pequeña todas las consultas facturan lo mismo" | §4 | **Razonable y demostrada en el laboratorio**: 966.472 B y 1.572.459 B facturan ambas 10.485.760 B. |
| *(inferencia)* "Si una partición entera cabe en un bloque, no hay bloques que saltarse" | §5 | **Razonable**: la doc dice que la colocación es por bloques y que los bloques se dimensionan según el tamaño de la tabla; y el propio laboratorio da 0 % de ahorro en una partición. |
| *(inferencia)* "los dos errores comparten forma: cambiar dos cosas a la vez" | §6 | **Razonable**, es metodología, no una afirmación sobre GCP. |
| ⚠️ "ninguna página fija un tamaño mínimo a partir del cual el clustering compense" | §5 | **Honesto y correcto**: no he encontrado tal umbral en `clustered-tables` ni en `partitioned-tables`; la única guía de tamaño es la de "approximately less than 10 GB", citada en el sentido correcto. |
| ⚠️ "Observado, no documentado: `bq` impersona con la variable de entorno…" | §7 | **Refutable con fuente** → hallazgo A-2. |

### Formato y nivel (criterio 6)

| Comprobación | Resultado |
|---|---|
| Quiz con callouts plegables `> [!question]-` | **OK**: las 8 preguntas usan `> [!question]-`. |
| Coherencia quiz ↔ cuerpo | **OK**: las 8 respuestas se corresponden con §3, §4, §5, §6, §7, §8 y §2; ninguna introduce datos que no estén en el cuerpo. |
| Nivel senior | **OK**: no explica qué es una partición, un índice o una columna; explica poda, `require_partition_filter`, mínimo facturable, caché y bloques de clustering, que es lo específico de BigQuery. |
| Nombres de servicio vigentes (2026) | **OK**: la lección solo nombra BigQuery, que no ha cambiado de nombre en la documentación consultada hoy. |

## Hallazgos

| Gravedad | Dónde (sección) | Qué | Evidencia |
|---|---|---|---|
| **Media** (A-1) | §7, 2º bullet | "El límite de 100 MB que documenta Google para ficheros locales es **el de la consola**" es una afirmación sobre la documentación **sin enlace**, y la página no figura en la lista de Fuentes. La afirmación es **correcta**. | `docs.cloud.google.com/bigquery/docs/loading-data-local` (act. 2026-09-03): "When using the Google Cloud console, files loaded from a local data source cannot exceed 100 MB." Basta añadir el enlace. |
| **Media** (A-2) | §7, último bullet | El ⚠️ dice "**no documentado**". La impersonación de `bq` **sí** está documentada, y el nombre de la variable de entorno se deriva de un patrón documentado. Solo la segunda mitad ("en `bq --help` no aparece ningún flag") es correcta. | bq-cli-reference (act. 2026-09-03): "To start running bq commands using service account impersonation, run the following command: `gcloud config set auth/impersonate_service_account SERVICE_ACCOUNT_NAME`". cloud.google.com/sdk/docs/properties: "The name of the environment variable follows the `CLOUDSDK_SECTION_NAME_PROPERTY_NAME` pattern." Comprobado: `bq --help` (v2.1.29) no contiene "impersonat". |
| Baja (B-1) | §2, párrafo tras la tabla | La frase "Las tres citas de la tabla son de Pricing" deja sin atribuir explícitamente la cuarta cita, que **no está en Pricing** sino en Editions (página sí enlazada en la sección). | 0 apariciones de "editions and the on-demand" en `cloud.google.com/bigquery/pricing`; literal en `editions-intro`. |
| Baja (B-2) | §3, "El hallazgo" | La cita elegida para justificar el fallo de `EXTRACT(MONTH FROM fecha)=7` ("There must be at least one predicate that **only references** a partitioning column…") no lo explica por sí sola: ese predicado **sí** referencia solo la columna de partición. Lo que lo explica es la lista cerrada de funciones que podan (`EXTRACT with DATE or YEAR parts`; `MONTH` no está), ya citada dos párrafos antes. La conclusión de la lección es correcta; el apoyo elegido es el más débil de los dos. | querying-partitioned-tables: lista de funciones + "Other functions and complex mathematical operations will require a full table scan." |
| Baja (B-3) | §3, "Qué la rompe" | "la aritmética sobre la columna" como caso que no poda es demasiado tajante: la doc admite envolver la columna en funciones soportadas con argumentos constantes, y su propio ejemplo `WHERE TIMESTAMP_ADD(datehour, INTERVAL 1 DAY) < '2025-01-03'` **sí** poda. | querying-partitioned-tables, "Isolate the partitioning column or use supported functions" + ejemplos. |
| Baja (B-4) | §5, línea bajo la tabla | "Los tamaños van en MB decimales, **como los cuenta BigQuery en `total_logical_bytes`**": `total_logical_bytes` está en **bytes** ("Total number of logical (uncompressed) bytes"), no en MB; y el almacenamiento se factura en **GiB binarios**. La conversión a MB decimales es del autor, no de BigQuery. Los números en sí son correctos. | `information-schema-table-storage`; pricing: "Storage usage is calculated in gibibytes months (GiB months), where 1 GiB is 2^30 bytes". |
| Informativo | Fuentes | La lista de Fuentes incluye **DDL** y **Quotas**, que no se citan en ningún punto del cuerpo (sí en los `.sql` del laboratorio), y **no incluye** `loading-data-local`, que sí haría falta (A-1). | Lectura de la lección y de `sql/02_tabla_transacciones.sql`. |
| Informativo | §7, 2º bullet | El job `LOAD` registrado duró **7 s** (12:16:55→12:17:03 UTC); "1 min 19 s" es tiempo de reloj del comando, dominado por la subida del fichero local. Coincide con `docs/progreso.md` (línea 210). No es un error, pero el texto podría precisar que es reloj de pared. | `INFORMATION_SCHEMA.JOBS_BY_PROJECT`: único job LOAD del proyecto, `dur_s = 7`. |

## Lo que no pude comprobar

- ⚠️ **El "1 min 19 s" no es reproducible hoy.** Solo puedo confirmar que el único job `LOAD` del proyecto duró
  7 s y que el CSV ocupa 172.802.717 bytes. La cifra de 1 min 19 s consta en `docs/progreso.md` como medida de
  reloj del laboratorio; la diferencia es coherente con la subida del fichero local, que no entra en la
  duración del job. No hay en `labs/paso-2` ningún artefacto que registre ese tiempo.
- ⚠️ **`total_logical_bytes` no lo pude leer directamente.** `INFORMATION_SCHEMA.TABLE_STORAGE` devolvió
  "User does not have permission to query table sdag-lab-000000:region-us.INFORMATION_SCHEMA.TABLE_STORAGE".
  Usé `bq show` (`numBytes = 162.765.153`) y la definición de la columna en la documentación. La conclusión de
  B-4 no depende de esa lectura.
- ⚠️ **El nombre literal `CLOUDSDK_AUTH_IMPERSONATE_SERVICE_ACCOUNT` no aparece deletreado** en ninguna página
  que haya encontrado: lo deduzco del patrón `CLOUDSDK_SECTION_NAME_PROPERTY_NAME` documentado por gcloud
  aplicado a la propiedad documentada `auth/impersonate_service_account`. Es una deducción de dos hechos
  documentados, no una cita.
- ⚠️ **La página de precios no lleva línea "Last updated"**, así que no puedo fechar su última actualización.
  La lección tampoco le atribuye fecha, de modo que no hay discrepancia. Los precios por región los extraje
  del propio HTML de la página (tabla incrustada de 48 regiones), no de un selector interactivo.
- ⚠️ **No verifiqué el contenido fila a fila de la tabla** ni reejecuté el generador: eso ya se cubrió en la
  verificación del laboratorio y queda fuera del criterio de esta lección.
